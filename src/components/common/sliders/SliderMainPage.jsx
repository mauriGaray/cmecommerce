import React from "react";
import Slider from "./slider";
const sliderMainPage = ({ items }) => {
  <>
    <Slider items={items} />
    <Slider items={items} />
    <Slider items={items} />
  </>;
};

export default sliderMainPage;
